<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
              aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'home' class='navbar-brand'}}Authentication Example{{/link-to}}
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        {{#if session.isAuthenticated}}
          <li><a {{ action 'invalidateSession' }}>Logout</a></li>
        {{else}}
          <li>{{#link-to 'login'}}Login{{/link-to}}</li>
        {{/if}}

        {{#if session.isAuthenticated}}
          <!-- admin menu items -->
          {{#if session.admin }}
            <li data-toggle="collapse" data-target=".navbar-collapse">{{#link-to 'admin.log'}}Logs{{/link-to}}</li>
          {{/if}}
        {{/if}}
      </ul>
    </div>
    <!--/.navbar-collapse -->
  </div>
</nav>

{{outlet}}



<div>
  is admin = {{session.admin}}
</div>